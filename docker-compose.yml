version: '3.8'

services:
  web_interface:
    build: ./web_server
    ports:
      - "80:8000"
    restart: always

  cloud_agent:
    build: ./agent
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    network_mode: "host" # Rejoint le réseau de la machine hôte
    restart: always